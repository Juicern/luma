# Luma Monorepo

This repo now contains:

- `backend/` – Go HTTP API (moved from repo root)
- `frontend-mac/` – placeholder for the macOS client (no code yet)

See `backend/README.md` for backend setup instructions.
